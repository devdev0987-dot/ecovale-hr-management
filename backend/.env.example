# ========================================
# Ecovale HR Backend - Environment Configuration
# ========================================
# 
# Copy this file to .env for local development
# For production, set these as system environment variables
#
# SECURITY: Never commit .env files to version control!
#

# ========================================
# DATABASE CONFIGURATION
# ========================================

# Database Host
# Local: localhost
# AWS RDS: your-db-instance.xxxx.us-east-1.rds.amazonaws.com
DB_HOST=localhost

# Database Port
# MySQL Default: 3306
DB_PORT=3306

# Database Name
DB_NAME=ecovale_hr

# Database Username
# Local: root (or your MySQL user)
# AWS RDS: admin (or IAM user)
DB_USERNAME=root

# Database Password
# IMPORTANT: Use strong passwords in production!
DB_PASSWORD=yourpassword

# SSL Configuration
# Local Development: false
# AWS RDS Production: true
DB_USE_SSL=false

# ========================================
# JPA / HIBERNATE CONFIGURATION
# ========================================

# DDL Auto Mode
# Development: update (auto-creates/updates tables)
# Production: validate (only validates schema, doesn't modify)
# Options: create, create-drop, update, validate, none
JPA_DDL_AUTO=update

# SQL Logging
# Development: true (helpful for debugging)
# Production: false (reduces log noise)
JPA_SHOW_SQL=false

# SQL Formatting
# Development: true (readable SQL in logs)
# Production: false (saves log space)
JPA_FORMAT_SQL=false

# ========================================
# APPLICATION CONFIGURATION (Optional)
# ========================================

# Server Port
# Default: 8080
# SERVER_PORT=8080

# Spring Profile
# Options: dev, prod, test
# SPRING_PROFILES_ACTIVE=dev

# Log Level
# Options: TRACE, DEBUG, INFO, WARN, ERROR
# LOGGING_LEVEL_ROOT=INFO
# LOGGING_LEVEL_COM_ECOVALE=DEBUG

# ========================================
# AWS RDS SPECIFIC CONFIGURATION
# ========================================

# Example AWS RDS MySQL Configuration:
# DB_HOST=ecovale-hr-db.c9xyz123.us-east-1.rds.amazonaws.com
# DB_PORT=3306
# DB_NAME=ecovale_hr_prod
# DB_USERNAME=admin
# DB_PASSWORD=your-secure-password-here
# DB_USE_SSL=true
# JPA_DDL_AUTO=validate

# ========================================
# PRODUCTION SECURITY NOTES
# ========================================
# 1. Use AWS Secrets Manager or Parameter Store for DB_PASSWORD
# 2. Enable SSL for RDS connections (DB_USE_SSL=true)
# 3. Use IAM database authentication when possible
# 4. Set JPA_DDL_AUTO=validate in production
# 5. Disable SQL logging (JPA_SHOW_SQL=false)
# 6. Use strong, unique passwords
# 7. Restrict database security groups to application servers only
# 8. Enable RDS encryption at rest
# 9. Enable automated backups and point-in-time recovery
# 10. Use RDS read replicas for high availability

# ========================================
# QUICK SETUP GUIDE
# ========================================
# 1. Copy this file: cp .env.example .env
# 2. Update DB_PASSWORD with your MySQL password
# 3. (Optional) Change DB_USERNAME if not using 'root'
# 4. Run: mvn spring-boot:run
# 5. Application will connect to database automatically

# ========================================
# AWS RDS SETUP CHECKLIST
# ========================================
# [ ] Create RDS MySQL instance in AWS Console
# [ ] Note the endpoint URL (DB_HOST)
# [ ] Create database: ecovale_hr
# [ ] Update security group to allow EC2/ECS access
# [ ] Set environment variables in EC2/ECS/Elastic Beanstalk
# [ ] Enable automated backups
# [ ] Enable encryption
# [ ] Configure monitoring and alarms
# [ ] Test connection from application server

# ========================================

# Application Flow - EcoVale HR

## 1. Application Entry Flow

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         APPLICATION STARTUP                              │
└─────────────────────────────────────────────────────────────────────────┘

                              ┌─────────────┐
                              │   App.jsx   │
                              └──────┬──────┘
                                     │
                      ┌──────────────┴──────────────┐
                      │  Check Authentication       │
                      │  (localStorage token)       │
                      └──────────────┬──────────────┘
                                     │
              ┌──────────────────────┴──────────────────────┐
              │                                              │
              ↓                                              ↓
    ┌─────────────────┐                           ┌─────────────────┐
    │   No Token      │                           │   Token Exists  │
    │   ↓             │                           │   ↓             │
    │   LoginPage     │                           │   Verify Token  │
    └─────────────────┘                           └────────┬────────┘
                                                           │
                                          ┌────────────────┴────────────────┐
                                          │                                  │
                                          ↓                                  ↓
                                ┌─────────────────┐              ┌─────────────────┐
                                │   Token Valid   │              │  Token Invalid  │
                                │   ↓             │              │   ↓             │
                                │   MainLayout    │              │   LoginPage     │
                                └─────────────────┘              └─────────────────┘
```

---

## 2. Authentication Flow

### 2.1 Login Process

```
User Action: Enter email/password, click Login

┌─────────────────────────────────────────────────────────────────────────┐
│                            LOGIN SEQUENCE                                │
└─────────────────────────────────────────────────────────────────────────┘

Step 1: Form Validation (Frontend)
        ├── Validate email format
        ├── Validate password length (min 6)
        └── Show errors if invalid

Step 2: API Request
        POST /api/auth/login
        Body: { email, password }

Step 3: Server Processing
        ├── Find user by email
        ├── Compare password hash (bcrypt)
        ├── Generate JWT tokens (access + refresh)
        └── Return tokens + user info

Step 4: Token Storage (Frontend)
        ├── Store access_token in localStorage
        ├── Store refresh_token in localStorage
        └── Store user info in context

Step 5: Redirect
        └── Navigate to Dashboard
```

### 2.2 Logout Process

```
User Action: Click Logout

Step 1: Clear localStorage
        ├── Remove access_token
        ├── Remove refresh_token
        └── Remove user info

Step 2: Clear React Context
        └── Reset auth state

Step 3: Redirect
        └── Navigate to Login page

(Optional) Step 4: Server Logout
        POST /api/auth/logout
        └── Invalidate refresh token on server
```

---

## 3. Main Navigation Flow

```
┌─────────────────────────────────────────────────────────────────────────┐
│                            MAIN LAYOUT                                   │
│                                                                          │
│  ┌──────────────┐  ┌─────────────────────────────────────────────────┐  │
│  │              │  │                    NAVBAR                        │  │
│  │              │  │  ┌─────────┐  ┌───────────────┐  ┌───────────┐  │  │
│  │              │  │  │ Menu ☰  │  │  Page Title   │  │  Logout   │  │  │
│  │              │  │  └─────────┘  └───────────────┘  └───────────┘  │  │
│  │   SIDEBAR    │  └─────────────────────────────────────────────────┘  │
│  │              │                                                        │
│  │  Dashboard   │  ┌─────────────────────────────────────────────────┐  │
│  │  Employees   │  │                                                  │  │
│  │  Designations│  │                 PAGE CONTENT                     │  │
│  │  Payroll     │  │                                                  │  │
│  │    ├─Payslip │  │           (Rendered based on route)             │  │
│  │    ├─Attend. │  │                                                  │  │
│  │    ├─Advance │  │                                                  │  │
│  │    ├─Loan    │  │                                                  │  │
│  │    └─PayRun  │  │                                                  │  │
│  │  Documents   │  │                                                  │  │
│  │  Settings    │  │                                                  │  │
│  │              │  └─────────────────────────────────────────────────┘  │
│  └──────────────┘                                                        │
└─────────────────────────────────────────────────────────────────────────┘
```

### Page Navigation Mapping

| Sidebar Item | Route | Page Component |
|--------------|-------|----------------|
| Dashboard | `/` or `/dashboard` | Dashboard.jsx |
| Employees | `/employees` | EmployeeList.jsx |
| Add Employee | `/employees/new` | EmployeeForm.jsx |
| Edit Employee | `/employees/:id/edit` | EmployeeForm.jsx |
| View Employee | `/employees/:id` | EmployeeDetail.jsx |
| Designations | `/designations` | Designations.jsx |
| Payslip | `/payroll/payslip` | Payslip.jsx |
| Attendance | `/payroll/attendance` | Attendance.jsx |
| Advance Register | `/payroll/advances` | Advances.jsx |
| Loan Register | `/payroll/loans` | Loans.jsx |
| Pay Run | `/payroll/payrun` | PayRun.jsx |
| Documents | `/documents` | Documents.jsx |
| Settings | `/settings` | Settings.jsx |

---

## 4. Employee Management Flow

### 4.1 Employee Listing

```
┌─────────────────────────────────────────────────────────────────────────┐
│                       EMPLOYEE LIST PAGE                                 │
└─────────────────────────────────────────────────────────────────────────┘

On Page Load:
├── Fetch employees: GET /api/employees
├── Apply default filters (status: all)
└── Render employee table

┌─────────────────────────────────────────────────────────────────────────┐
│  ┌──────────────────────────────────────────┐  ┌──────────────────────┐ │
│  │ 🔍 Search by name or ID...              │  │  + Add Employee      │ │
│  └──────────────────────────────────────────┘  └──────────────────────┘ │
│                                                                          │
│  ┌────────────────────────────────────────────────────────────────────┐ │
│  │ ID │ Name        │ Type      │ Designation  │ Join Date │ Actions │ │
│  ├────┼─────────────┼───────────┼──────────────┼───────────┼─────────┤ │
│  │ 1  │ Alice John  │ Full Time │ Sr Engineer  │ 15/03/20  │ ⋮ 👁️ ✏️ 🗑 │ │
│  │ 2  │ Bob Smith   │ Full Time │ Accountant   │ 01/07/22  │ ⋮ 👁️ ✏️ 🗑 │ │
│  │ 3  │ Priya Sharma│ Full Time │ HR Manager   │ 10/01/19  │ ⋮ 👁️ ✏️ 🗑 │ │
│  └────┴─────────────┴───────────┴──────────────┴───────────┴─────────┘ │
│                                                                          │
│  ┌─────────────────────────────────────────────────────────────────────┐│
│  │ Showing 1-10 of 50 employees            ◀ 1 2 3 4 5 ▶              ││
│  └─────────────────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────────────────┘

Actions:
├── Search: Filter by name/ID (debounced, 300ms)
├── Add Employee: Navigate to /employees/new
├── View (👁️): Navigate to /employees/:id
├── Edit (✏️): Navigate to /employees/:id/edit
├── Delete (🗑): Confirm → DELETE /api/employees/:id → Refresh list
└── Pagination: Page change → Re-fetch with ?page=X&limit=Y
```

### 4.2 Create Employee

```
┌─────────────────────────────────────────────────────────────────────────┐
│                      CREATE EMPLOYEE FORM                                │
└─────────────────────────────────────────────────────────────────────────┘

Form Sections:
├── Personal Information
│   ├── First Name*, Last Name*, Middle Name
│   ├── Gender*, Date of Birth
│   ├── Contact Number*, Alternate Contact
│   ├── Personal Email*, Emergency Contact
│   ├── Current Address*, Permanent Address
│   ├── Photo Upload, Blood Group
│   ├── Father Name, Mother Name
│   └── PF Number, ESI Number
│
├── Employment Details
│   ├── Employment Type* (Full-time/Part-time)
│   ├── Department*, Designation*
│   ├── Official Email (auto-generated)
│   ├── Work Location*, Join Date
│   ├── Probation Period, Grade
│   └── Reporting Manager
│
└── Salary Information
    ├── CTC (Annual)* → Auto-calculate
    ├── HRA Percentage* (default 10%)
    ├── Allowances (Conveyance, Telephone, Medical)
    ├── Special Allowance (calculated)
    ├── PF Enable (checkbox) → Auto-calculate
    ├── ESI Enable (checkbox) → Auto-calculate
    ├── Professional Tax, TDS
    ├── Payment Mode (Bank/Cash/Cheque)
    └── Bank Details (if Bank selected)

* Required fields

Form Flow:
┌─────────────────────────────────────────────────────────────────────────┐
│                                                                          │
│  [Personal Info Tab] → [Employment Tab] → [Salary Tab] → [Review]       │
│                                                                          │
│  User fills Personal Info                                                │
│         ↓                                                                │
│  Click "Next" → Validate → Show Employment Tab                          │
│         ↓                                                                │
│  User fills Employment Details                                           │
│         ↓                                                                │
│  Click "Next" → Validate → Show Salary Tab                              │
│         ↓                                                                │
│  User enters CTC → Auto-calculate all salary components                 │
│         ↓                                                                │
│  Click "Save" → Validate All → POST /api/employees → Success Toast      │
│         ↓                                                                │
│  Redirect to Employee List                                               │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

### 4.3 Salary Calculation Flow

```
User enters CTC: 1,200,000 (12 LPA)

┌─────────────────────────────────────────────────────────────────────────┐
│                    SALARY CALCULATION ENGINE                             │
└─────────────────────────────────────────────────────────────────────────┘

Step 1: Calculate Basic
        Basic (monthly) = CTC × 0.50 / 12
        Basic = 1,200,000 × 0.50 / 12 = ₹50,000

Step 2: Calculate HRA
        HRA = Basic × HRA%
        HRA = 50,000 × 0.10 = ₹5,000

Step 3: Fixed Allowances
        Conveyance = ₹1,600 (user input)
        Telephone = ₹500 (user input)
        Medical = ₹1,250 (user input)

Step 4: Calculate Employer Contributions
        PF Wage = min(Basic, 15000) = ₹15,000
        Employer PF = PF Wage × 0.12 = ₹1,800
        
        (If ESI enabled and Gross < 21000)
        Employer ESI = Gross × 0.0325

Step 5: Calculate Special Allowance (Balancing)
        Monthly CTC = Annual CTC / 12 = ₹100,000
        
        Special = Monthly CTC - Basic - HRA - Allowances - Employer Contributions
        Special = 100,000 - 50,000 - 5,000 - 3,350 - 1,800 = ₹39,850

Step 6: Calculate Gross
        Gross = Basic + HRA + Conveyance + Telephone + Medical + Special
        Gross = 50,000 + 5,000 + 1,600 + 500 + 1,250 + 39,850 = ₹98,200

Step 7: Calculate Deductions
        Employee PF = min(Basic, 15000) × 0.12 = ₹1,800
        
        (If ESI enabled and Gross < 21000)
        Employee ESI = Gross × 0.0075
        
        Professional Tax = (Gross > 25000) ? ₹200 : ₹0 = ₹200
        
        TDS = Gross × TDS% (user input)

Step 8: Calculate Net
        Net = Gross - Employee PF - Employee ESI - PT - TDS
        Net = 98,200 - 1,800 - 0 - 200 - 0 = ₹96,200

┌─────────────────────────────────────────────────────────────────────────┐
│  SALARY BREAKDOWN DISPLAY                                                │
│  ┌─────────────────────────────┬─────────────────────────────┐          │
│  │ Earnings                    │ Deductions                  │          │
│  ├─────────────────────────────┼─────────────────────────────┤          │
│  │ Basic         : ₹50,000    │ Employee PF   : ₹1,800     │          │
│  │ HRA           : ₹5,000     │ Employee ESI  : ₹0         │          │
│  │ Conveyance    : ₹1,600     │ Professional Tax: ₹200     │          │
│  │ Telephone     : ₹500       │ TDS           : ₹0         │          │
│  │ Medical       : ₹1,250     │                             │          │
│  │ Special       : ₹39,850    │                             │          │
│  ├─────────────────────────────┼─────────────────────────────┤          │
│  │ Gross         : ₹98,200    │ Total Deductions: ₹2,000   │          │
│  ├─────────────────────────────┴─────────────────────────────┤          │
│  │ NET SALARY    : ₹96,200                                   │          │
│  │ ANNUAL CTC    : ₹12,00,000                                │          │
│  └───────────────────────────────────────────────────────────┘          │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 5. Attendance Management Flow

### 5.1 Record Attendance

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    ATTENDANCE REGISTER PAGE                              │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│  Filter: [Select Month ▼]  [Select Year ▼]  [Select Employee ▼]        │
│                                                                          │
│  ┌────────────────────────────────────────────────────────────────────┐ │
│  │ Employee      │ Month  │ Working │ Present │ Absent │ Leave │ LOP │ │
│  ├───────────────┼────────┼─────────┼─────────┼────────┼───────┼─────┤ │
│  │ Alice Johnson │ Jan 26 │   26    │   24    │   0    │   2   │  0  │ │
│  │ Bob Smith     │ Jan 26 │   26    │   22    │   2    │   2   │  2  │ │
│  │ Priya Sharma  │ Jan 26 │   26    │   26    │   0    │   0   │  0  │ │
│  └───────────────┴────────┴─────────┴─────────┴────────┴───────┴─────┘ │
│                                                                          │
│                                     [+ Add Attendance Record]            │
└─────────────────────────────────────────────────────────────────────────┘

Add/Edit Attendance Form:
┌─────────────────────────────────────────────────────────────────────────┐
│  Employee*:        [Select Employee ▼]                                   │
│  Month*:           [January ▼]  Year*: [2026 ▼]                         │
│  Total Working Days*: [26]                                               │
│  Present Days*:    [24]                                                  │
│  Absent Days*:     [2]                                                   │
│  Paid Leave:       [0]                                                   │
│  Unpaid Leave:     [0]                                                   │
│  Remarks:          [____________________]                                │
│                                                                          │
│  ┌─────────────────────────────────────────────────────────────────────┐│
│  │ Auto-Calculated:                                                     ││
│  │ Payable Days = Present Days + Paid Leave = 24 + 0 = 24              ││
│  │ LOP Days = Unpaid Leave + Absent Days = 0 + 2 = 2                   ││
│  └─────────────────────────────────────────────────────────────────────┘│
│                                                                          │
│                               [Cancel]  [Save]                           │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 6. Payroll Processing Flow

### 6.1 Pay Run Generation

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         PAY RUN PAGE                                     │
└─────────────────────────────────────────────────────────────────────────┘

Step 1: Select Period
┌─────────────────────────────────────────────────────────────────────────┐
│  Generate Pay Run for:  [January ▼]  [2026 ▼]    [▶ Generate Pay Run]  │
└─────────────────────────────────────────────────────────────────────────┘

Step 2: System Processing
┌─────────────────────────────────────────────────────────────────────────┐
│  Processing...                                                           │
│  ├── Fetching active employees                                          │
│  ├── Fetching attendance records for Jan 2026                           │
│  ├── Fetching pending advances for Jan 2026                             │
│  ├── Fetching active loan EMIs for Jan 2026                             │
│  ├── Calculating salary for each employee                               │
│  │   ├── Pro-rating based on attendance                                 │
│  │   ├── Applying LOP deductions                                        │
│  │   ├── Applying advance deductions                                    │
│  │   ├── Applying loan EMI deductions                                   │
│  │   └── Calculating net pay                                            │
│  └── Saving pay run record                                              │
└─────────────────────────────────────────────────────────────────────────┘

Step 3: Display Results
┌─────────────────────────────────────────────────────────────────────────┐
│  Pay Run: January 2026                          [📥 Export CSV]         │
│  Generated: 19/01/2026 10:30 AM                                         │
│                                                                          │
│  ┌────────────────────────────────────────────────────────────────────┐ │
│  │ Summary                                                             │ │
│  │ ├── Total Employees: 6                                              │ │
│  │ ├── Total Gross: ₹5,23,200                                          │ │
│  │ ├── Total Deductions: ₹42,600                                       │ │
│  │ └── Total Net Pay: ₹4,80,600                                        │ │
│  └────────────────────────────────────────────────────────────────────┘ │
│                                                                          │
│  ┌────────────────────────────────────────────────────────────────────┐ │
│  │ Employee      │ Gross    │ LOP    │ Advances│ Loans │ Net Pay     │ │
│  ├───────────────┼──────────┼────────┼─────────┼───────┼─────────────┤ │
│  │ Alice Johnson │ 1,30,000 │ 0      │ 0       │ 0     │ 1,12,533    │ │
│  │ Bob Smith     │ 55,385   │ 4,615  │ 5,000   │ 2,000 │ 43,770      │ │
│  │ Priya Sharma  │ 1,00,000 │ 0      │ 0       │ 0     │ 88,383      │ │
│  └───────────────┴──────────┴────────┴─────────┴───────┴─────────────┘ │
└─────────────────────────────────────────────────────────────────────────┘
```

### 6.2 Pay Run Calculation Logic

```
For each active employee:
┌─────────────────────────────────────────────────────────────────────────┐
│                     PAY RUN CALCULATION                                  │
└─────────────────────────────────────────────────────────────────────────┘

1. Get Employee Salary Info
   ├── Basic, HRA, Allowances from employee record
   └── Statutory flags (PF, ESI enabled)

2. Get Attendance Record
   ├── Total Working Days (e.g., 26)
   ├── Payable Days = Present + Paid Leave
   └── LOP Days = Unpaid Leave + Absent

3. Pro-rate Salary Based on Attendance
   ├── Salary Per Day = Basic / Total Working Days
   ├── LOP Amount = LOP Days × Salary Per Day
   ├── Adjusted Basic = Basic - LOP Amount
   └── Allowance Ratio = Payable Days / Total Working Days
       ├── Adjusted HRA = HRA × Ratio
       ├── Adjusted Conveyance = Conveyance × Ratio
       └── ... (all allowances)

4. Get Deductions
   ├── Advance Deduction (from advance register, status: pending)
   ├── Loan EMI (from loan register, active loans, pending EMIs for month)
   ├── PF = min(Adjusted Basic, 15000) × 0.12
   ├── ESI = Gross × 0.0075 (if applicable)
   ├── PT = 200 (if gross > 25000)
   └── TDS = Gross × TDS%

5. Calculate Net Pay
   Net = Adjusted Gross - All Deductions

6. Create PayRunEmployeeRecord
   {
     employeeId, employeeName,
     basicSalary, hra, conveyance, telephone, medical, special,
     grossSalary, totalWorkingDays, payableDays, lossOfPayDays,
     lossOfPayAmount, advanceDeduction, loanDeduction,
     pfDeduction, esiDeduction, professionalTax, tds,
     totalDeductions, netPay
   }
```

---

## 7. Advance Management Flow

```
┌─────────────────────────────────────────────────────────────────────────┐
│                      ADVANCE REGISTER FLOW                               │
└─────────────────────────────────────────────────────────────────────────┘

1. Create Advance
   ├── Select Employee
   ├── Enter Advance Month (when paid)
   ├── Enter Advance Amount
   ├── Select Deduction Month (when to deduct)
   └── Save → Status: pending

2. Advance in Pay Run
   ├── Pay Run for Deduction Month
   ├── Find pending advances for employee
   ├── Add to deductions
   └── Update advance status: deducted

Example:
┌─────────────────────────────────────────────────────────────────────────┐
│  Employee: Bob Smith                                                     │
│  Advance Paid: January 2026 - ₹5,000                                    │
│  Deduction Month: February 2026                                          │
│  Status: pending                                                         │
│                                                                          │
│  → February Pay Run generates                                            │
│  → Bob's advance of ₹5,000 deducted                                     │
│  → Advance status: deducted                                              │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 8. Loan Management Flow

```
┌─────────────────────────────────────────────────────────────────────────┐
│                       LOAN REGISTER FLOW                                 │
└─────────────────────────────────────────────────────────────────────────┘

1. Create Loan
   ├── Select Employee
   ├── Enter Loan Amount: ₹60,000
   ├── Enter Interest Rate: 0% (usually no interest)
   ├── Enter Number of EMIs: 12
   ├── Select Start Month: January 2026
   └── Auto-generate EMI Schedule

2. EMI Schedule Generation
   ├── Total Amount = Loan + Interest
   ├── EMI = Total / Number of EMIs
   └── Generate EMI records for each month

   Example:
   ┌──────────────┬─────────┬─────────┐
   │ Month        │ EMI     │ Status  │
   ├──────────────┼─────────┼─────────┤
   │ January 2026 │ ₹5,000  │ pending │
   │ February 2026│ ₹5,000  │ pending │
   │ March 2026   │ ₹5,000  │ pending │
   │ ...          │ ...     │ ...     │
   │ December 2026│ ₹5,000  │ pending │
   └──────────────┴─────────┴─────────┘

3. EMI in Pay Run
   ├── Pay Run for January 2026
   ├── Find active loans with pending EMI for January
   ├── Add EMI to deductions
   └── Update EMI status: paid, record paid date

4. Loan Completion
   ├── All EMIs paid
   ├── Remaining Balance = 0
   └── Loan status: completed
```

---

## 9. State Management

### 9.1 Global State (React Context)

```javascript
// context/AppContext.jsx

const AppContext = createContext();

function AppContextProvider({ children }) {
  const [user, setUser] = useState(null);
  const [isLoading, setIsLoading] = useState(false);
  
  // Toast notifications
  const [toasts, setToasts] = useState([]);
  
  const showToast = useCallback((message, type = 'info') => {
    const id = Date.now();
    setToasts(prev => [...prev, { id, message, type }]);
    setTimeout(() => {
      setToasts(prev => prev.filter(t => t.id !== id));
    }, 5000);
  }, []);
  
  const value = {
    user,
    setUser,
    isLoading,
    setIsLoading,
    showToast
  };
  
  return (
    <AppContext.Provider value={value}>
      {children}
      <ToastContainer toasts={toasts} />
    </AppContext.Provider>
  );
}
```

### 9.2 Server State (React Query)

```javascript
// hooks/useEmployees.js
import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
import employeeService from '../services/employeeService';

// Fetch employees
export function useEmployees(filters) {
  return useQuery({
    queryKey: ['employees', filters],
    queryFn: () => employeeService.getEmployees(filters)
  });
}

// Create employee
export function useCreateEmployee() {
  const queryClient = useQueryClient();
  
  return useMutation({
    mutationFn: employeeService.createEmployee,
    onSuccess: () => {
      queryClient.invalidateQueries(['employees']);
    }
  });
}

// Update employee
export function useUpdateEmployee() {
  const queryClient = useQueryClient();
  
  return useMutation({
    mutationFn: ({ id, data }) => employeeService.updateEmployee(id, data),
    onSuccess: (_, { id }) => {
      queryClient.invalidateQueries(['employees']);
      queryClient.invalidateQueries(['employee', id]);
    }
  });
}

// Delete employee
export function useDeleteEmployee() {
  const queryClient = useQueryClient();
  
  return useMutation({
    mutationFn: employeeService.deleteEmployee,
    onSuccess: () => {
      queryClient.invalidateQueries(['employees']);
    }
  });
}
```

---

## 10. Error Handling Flow

```
┌─────────────────────────────────────────────────────────────────────────┐
│                       ERROR HANDLING FLOW                                │
└─────────────────────────────────────────────────────────────────────────┘

Frontend Error Types:
├── Validation Error → Show inline field errors
├── API Error (4xx) → Show error toast
├── Network Error → Show connection error
├── Auth Error (401) → Redirect to login
└── Server Error (5xx) → Show generic error

Example Implementation:
┌─────────────────────────────────────────────────────────────────────────┐
│  // services/api.js                                                      │
│                                                                          │
│  api.interceptors.response.use(                                          │
│    response => response,                                                 │
│    error => {                                                            │
│      if (error.response?.status === 401) {                              │
│        // Clear auth and redirect                                        │
│        localStorage.removeItem('token');                                 │
│        window.location.href = '/login';                                  │
│      }                                                                   │
│      return Promise.reject(error);                                       │
│    }                                                                     │
│  );                                                                      │
└─────────────────────────────────────────────────────────────────────────┘
```

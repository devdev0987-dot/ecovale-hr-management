# Frontend JWT Implementation - Visual Guide

## 🎯 Complete Implementation Overview

```
┌───────────────────────────────────────────────────────────────┐
│                    FRONTEND (React App)                        │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │              1. AuthProvider (Global State)              │  │
│  │  ┌─────────────────────────────────────────────────┐    │  │
│  │  │  contexts/AuthContext.jsx                        │    │  │
│  │  │  • user: { id, username, email, roles }         │    │  │
│  │  │  • isAuthenticated: boolean                     │    │  │
│  │  │  • login(username, password)                    │    │  │
│  │  │  • logout()                                      │    │  │
│  │  │  • hasRole(role)                                │    │  │
│  │  └─────────────────────────────────────────────────┘    │  │
│  └──────────────────────────┬──────────────────────────────┘  │
│                             │                                  │
│  ┌──────────────────────────▼──────────────────────────────┐  │
│  │              2. Authentication Components              │  │
│  │  ┌─────────────────────┐  ┌──────────────────────────┐ │  │
│  │  │  components/        │  │  components/             │ │  │
│  │  │  Login.jsx          │  │  ProtectedRoute.jsx      │ │  │
│  │  │                     │  │                          │ │  │
│  │  │  • Form validation  │  │  • Check isAuth()        │ │  │
│  │  │  • Error handling   │  │  • Check roles           │ │  │
│  │  │  • Loading states   │  │  • Redirect if needed    │ │  │
│  │  │  • Calls authSvc    │  │  • Show fallback UI      │ │  │
│  │  └──────────┬──────────┘  └──────────────────────────┘ │  │
│  └─────────────┼────────────────────────────────────────────┘  │
│                │                                                │
│  ┌─────────────▼────────────────────────────────────────────┐  │
│  │              3. Authentication Service                   │  │
│  │  ┌──────────────────────────────────────────────────┐   │  │
│  │  │  services/authService.js                          │   │  │
│  │  │                                                    │   │  │
│  │  │  login(username, password)                        │   │  │
│  │  │    → POST /api/auth/login                         │   │  │
│  │  │    → Store token in localStorage                  │   │  │
│  │  │    → Store user data in localStorage              │   │  │
│  │  │                                                    │   │  │
│  │  │  logout()                                          │   │  │
│  │  │    → Clear localStorage                           │   │  │
│  │  │    → Redirect to login                            │   │  │
│  │  │                                                    │   │  │
│  │  │  isAuthenticated()                                │   │  │
│  │  │    → Check token exists                           │   │  │
│  │  │    → Validate token expiration                    │   │  │
│  │  │                                                    │   │  │
│  │  │  getToken() → Read from localStorage              │   │  │
│  │  │  getUser() → Parse from localStorage              │   │  │
│  │  │  hasRole(role) → Check user.roles[]               │   │  │
│  │  │  isAdmin() → Check ROLE_ADMIN                     │   │  │
│  │  └──────────────────────────────────────────────────┘   │  │
│  └─────────────┬────────────────────────────────────────────┘  │
│                │                                                │
│  ┌─────────────▼────────────────────────────────────────────┐  │
│  │              4. API Client with Interceptors            │  │
│  │  ┌──────────────────────────────────────────────────┐   │  │
│  │  │  services/apiClient.js (Axios)                    │   │  │
│  │  │                                                    │   │  │
│  │  │  REQUEST INTERCEPTOR:                             │   │  │
│  │  │  ┌──────────────────────────────────────────┐    │   │  │
│  │  │  │ 1. Get token from localStorage           │    │   │  │
│  │  │  │ 2. Add Authorization: Bearer <token>     │    │   │  │
│  │  │  │ 3. Send request                          │    │   │  │
│  │  │  └──────────────────────────────────────────┘    │   │  │
│  │  │                                                    │   │  │
│  │  │  RESPONSE INTERCEPTOR:                            │   │  │
│  │  │  ┌──────────────────────────────────────────┐    │   │  │
│  │  │  │ If 200 OK → Return data                  │    │   │  │
│  │  │  │ If 401 Unauthorized →                    │    │   │  │
│  │  │  │   • Clear localStorage                   │    │   │  │
│  │  │  │   • Redirect to login                    │    │   │  │
│  │  │  │ If 403 Forbidden →                       │    │   │  │
│  │  │  │   • Show access denied error             │    │   │  │
│  │  │  │ If Network Error →                       │    │   │  │
│  │  │  │   • Show network error                   │    │   │  │
│  │  │  └──────────────────────────────────────────┘    │   │  │
│  │  └──────────────────────────────────────────────────┘   │  │
│  └─────────────┬────────────────────────────────────────────┘  │
└────────────────┼─────────────────────────────────────────────────┘
                 │
                 │ HTTP Requests
                 │ Authorization: Bearer <JWT>
                 │
┌────────────────▼─────────────────────────────────────────────────┐
│                    BACKEND (Spring Boot)                          │
│                                                                    │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  JWT Authentication Filter                                │   │
│  │  • Extract token from Authorization header               │   │
│  │  • Validate signature                                     │   │
│  │  • Check expiration                                       │   │
│  │  • Load user from database                                │   │
│  │  • Set SecurityContext                                    │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                    │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  Controllers with @PreAuthorize                           │   │
│  │  • EmployeeController - CRUD with role checks            │   │
│  │  • AttendanceController - CRUD with role checks          │   │
│  │  • LoanController - CRUD with role checks                │   │
│  └──────────────────────────────────────────────────────────┘   │
└────────────────────────────────────────────────────────────────────┘
```

## 📱 User Flow Diagrams

### Login Flow
```
┌─────────┐
│  User   │
└────┬────┘
     │
     │ 1. Enter username & password
     ▼
┌──────────────┐
│  Login.jsx   │
└──────┬───────┘
       │
       │ 2. Validate form
       ▼
┌─────────────────┐
│  authService    │
│  .login()       │
└──────┬──────────┘
       │
       │ 3. POST /api/auth/login
       ▼
┌─────────────────┐
│  Backend        │
│  AuthController │
└──────┬──────────┘
       │
       │ 4. Validate credentials
       │ 5. Generate JWT token
       ▼
┌─────────────────┐
│  Response       │
│  {              │
│   token: "...", │
│   user: {...}   │
│  }              │
└──────┬──────────┘
       │
       │ 6. Store in localStorage
       ▼
┌──────────────────┐
│  localStorage    │
│  • auth_token    │
│  • auth_user     │
└──────┬───────────┘
       │
       │ 7. Update AuthContext
       ▼
┌──────────────────┐
│  AuthContext     │
│  • isAuth = true │
│  • user = {...}  │
└──────┬───────────┘
       │
       │ 8. Redirect
       ▼
┌──────────────────┐
│  Dashboard       │
└──────────────────┘
```

### Protected Page Access Flow
```
┌─────────┐
│  User   │
└────┬────┘
     │
     │ Navigate to /employees
     ▼
┌───────────────────┐
│ ProtectedRoute    │
└────┬──────────────┘
     │
     │ Check isAuthenticated()
     ├─────────┬─────────┐
     │ FALSE   │  TRUE   │
     ▼         ▼         │
┌────────┐ ┌──────────┐ │
│Redirect│ │Check Role│ │
│to Login│ │Required? │ │
└────────┘ └────┬─────┘ │
                │       │
           ┌────┴───┐   │
           │ YES    │ NO│
           ▼        ▼   │
      ┌─────────┐ ┌────▼────┐
      │Has Role?│ │ Render  │
      └────┬────┘ │ Content │
           │      └─────────┘
      ┌────┴────┐
      │ YES │NO │
      ▼     ▼   │
   ┌──────┐ ┌──────────┐
   │Render│ │Access    │
   │Page  │ │Denied UI │
   └──────┘ └──────────┘
```

### API Request Flow with JWT
```
┌──────────────┐
│  Component   │
└──────┬───────┘
       │
       │ apiClient.get('/employees')
       ▼
┌────────────────────┐
│  Request           │
│  Interceptor       │
└──────┬─────────────┘
       │
       │ 1. Get token from localStorage
       │ 2. Add header: Authorization: Bearer <token>
       ▼
┌────────────────────┐
│  Send HTTP Request │
│  GET /employees    │
│  Authorization:    │
│  Bearer eyJhbGc... │
└──────┬─────────────┘
       │
       ▼
┌────────────────────┐
│  Backend           │
│  JWT Filter        │
└──────┬─────────────┘
       │
       │ 1. Extract token
       │ 2. Validate signature
       │ 3. Check expiration
       │ 4. Load user
       │ 5. Check @PreAuthorize
       ▼
┌────────────────────┐
│  Controller        │
│  @PreAuthorize     │
│  hasAnyRole(...)   │
└──────┬─────────────┘
       │
       ▼
  ┌────┴─────┐
  │ Allowed? │
  └────┬─────┘
       │
  ┌────┴────┐
  │YES  │NO │
  ▼     ▼   │
┌─────┐ ┌──────┐
│ 200 │ │ 403  │
│ OK  │ │Forbid│
└──┬──┘ └───┬──┘
   │        │
   │        │ Response Interceptor
   │        │ • Show error
   │        │ • Don't logout (403)
   │        │
   ▼        ▼
┌─────────────────┐
│  Return Data    │
│  to Component   │
└─────────────────┘
```

### Token Expiration Handling
```
┌──────────────┐
│  API Request │
└──────┬───────┘
       │
       │ With expired token
       ▼
┌────────────────────┐
│  Backend           │
│  JWT Filter        │
└──────┬─────────────┘
       │
       │ Token expired!
       ▼
┌────────────────────┐
│  Return 401        │
│  Unauthorized      │
└──────┬─────────────┘
       │
       ▼
┌────────────────────┐
│  Response          │
│  Interceptor       │
└──────┬─────────────┘
       │
       │ 1. Detect 401 status
       │ 2. Clear localStorage
       │ 3. Redirect to login
       ▼
┌────────────────────┐
│  Login Page        │
│  "Session expired" │
└────────────────────┘
```

## 🗂️ File Organization

```
ecovale-hr-web-app/
│
├── components/
│   ├── Login.jsx                    ✅ NEW - Login page
│   ├── ProtectedRoute.jsx           ✅ NEW - Route protection
│   ├── layout/
│   │   ├── MainLayout.tsx
│   │   ├── Navbar.tsx               ⚠️  UPDATE - Add logout button
│   │   └── Sidebar.tsx
│   └── ui/
│       ├── Button.tsx
│       └── ...
│
├── contexts/
│   ├── AuthContext.jsx              ✅ NEW - Global auth state
│   └── AppContext.tsx               (existing)
│
├── services/
│   ├── authService.js               ✅ NEW - Auth operations
│   ├── apiClient.js                 ✅ UPDATED - JWT interceptors
│   ├── employeeService.js           (existing)
│   └── ...
│
├── pages/
│   ├── LoginPage.tsx                ⚠️  REPLACE with Login.jsx
│   ├── EmployeesPage.tsx            ⚠️  WRAP with ProtectedRoute
│   ├── DashboardPage.tsx            ⚠️  WRAP with ProtectedRoute
│   └── ...                          ⚠️  WRAP all pages
│
├── FRONTEND-JWT-GUIDE.md            ✅ NEW - Full documentation
├── FRONTEND-JWT-CHECKLIST.md        ✅ NEW - Integration steps
├── FRONTEND-JWT-SUMMARY.md          ✅ NEW - Implementation summary
└── FRONTEND-JWT-VISUAL.md           ✅ NEW - This file
```

## 🔄 Data Flow

### localStorage Structure
```javascript
// After successful login
localStorage = {
  "auth_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "auth_user": {
    "id": 1,
    "username": "admin",
    "email": "admin@ecovale.com",
    "fullName": "Admin User",
    "roles": ["ROLE_ADMIN"],
    "type": "Bearer"
  }
}
```

### JWT Token Structure
```
eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiJhZG1pbiIsImlhdCI6MTcwNjI3NDAwMCwiZXhwIjoxNzA2MzYwNDAwfQ.signature

┌─────────────────┐ ┌─────────────────────────────┐ ┌──────────┐
│     Header      │ │          Payload             │ │Signature │
│  {              │ │  {                           │ │          │
│   "alg":"HS256",│ │   "sub": "admin",            │ │  HMAC    │
│   "typ":"JWT"   │ │   "iat": 1706274000,         │ │  SHA256  │
│  }              │ │   "exp": 1706360400          │ │          │
│                 │ │  }                           │ │          │
└─────────────────┘ └─────────────────────────────┘ └──────────┘
     Base64             Base64                         Base64
```

## 🎨 UI Components

### Login Page (Login.jsx)
```
┌──────────────────────────────────────────┐
│                                           │
│           🏢 Ecovale HR                   │
│      Sign in to your account              │
│                                           │
│  ┌─────────────────────────────────────┐ │
│  │  Username                            │ │
│  │  ┌─────────────────────────────────┐│ │
│  │  │ Enter your username             ││ │
│  │  └─────────────────────────────────┘│ │
│  │                                      │ │
│  │  Password                            │ │
│  │  ┌─────────────────────────────────┐│ │
│  │  │ ●●●●●●●●●●                      ││ │
│  │  └─────────────────────────────────┘│ │
│  │                                      │ │
│  │  [ ] Remember me    Forgot password?│ │
│  │                                      │ │
│  │  ┌─────────────────────────────────┐│ │
│  │  │         Sign in                 ││ │
│  │  └─────────────────────────────────┘│ │
│  │                                      │ │
│  │  Don't have an account?              │ │
│  │  Contact Administrator               │ │
│  └─────────────────────────────────────┘ │
│                                           │
│      Ecovale HR Management System v1.0   │
└──────────────────────────────────────────┘
```

### Protected Route - Access Denied
```
┌──────────────────────────────────────────┐
│                                           │
│                  🔒                       │
│                                           │
│            Access Denied                  │
│                                           │
│   You don't have permission to access     │
│           this page.                      │
│                                           │
│          ┌──────────────┐                │
│          │   Go Back    │                │
│          └──────────────┘                │
│                                           │
└──────────────────────────────────────────┘
```

## 📊 State Management

### AuthContext State Tree
```
AuthContext
├── user: Object | null
│   ├── id: number
│   ├── username: string
│   ├── email: string
│   ├── fullName: string
│   └── roles: string[]
├── isAuthenticated: boolean
├── isLoading: boolean
└── methods:
    ├── login(username, password)
    ├── logout()
    ├── hasRole(role)
    ├── hasAnyRole(roles)
    ├── isAdmin()
    ├── getDisplayName()
    └── refreshAuth()
```

## 🔐 Security Layers

```
┌─────────────────────────────────────────────┐
│  Layer 1: Client-Side Token Validation     │
│  • authService.isAuthenticated()           │
│  • Check token exists                       │
│  • Validate expiration (basic check)       │
└──────────────────┬──────────────────────────┘
                   │
┌──────────────────▼──────────────────────────┐
│  Layer 2: Component-Level Protection        │
│  • ProtectedRoute wrapper                   │
│  • requireRole prop checking                │
│  • Redirect if unauthorized                 │
└──────────────────┬──────────────────────────┘
                   │
┌──────────────────▼──────────────────────────┐
│  Layer 3: API Request Interception          │
│  • Automatic token injection                │
│  • 401/403 error handling                   │
│  • Auto-logout on auth failure              │
└──────────────────┬──────────────────────────┘
                   │
┌──────────────────▼──────────────────────────┐
│  Layer 4: Backend JWT Validation            │
│  • JwtAuthenticationFilter                  │
│  • Signature verification                   │
│  • Expiration checking                      │
│  • User loading from database               │
└──────────────────┬──────────────────────────┘
                   │
┌──────────────────▼──────────────────────────┐
│  Layer 5: Method-Level Authorization        │
│  • @PreAuthorize annotations                │
│  • Role-based access control                │
│  • Spring Security enforcement              │
└─────────────────────────────────────────────┘
```

---

**Created**: January 26, 2025  
**Purpose**: Visual guide for JWT implementation  
**Status**: Complete
